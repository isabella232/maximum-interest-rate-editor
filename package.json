{
  "name": "alma-maximum-interest-rate-editor",
  "version": "1.0.0",
  "description": "Alma Maximum Interest Rate Editor",
  "scripts": {
    "analyse": "elm-analyse",
    "build": "npm run cleanup && npm run copy-assets && elm make src/MaximumInterestRateEditor.elm --output=build/app.js --optimize",
    "build-with-taeg": "npm run cleanup && npm run copy-assets && elm make src/MaximumInterestRateEditor.elm src/TAEG.elm --output=build/app.js --optimize",
    "copy-assets": "rimraf build && mkdir build && cp -r public/* build",
    "cleanup": "rimraf elm-stuff/",
    "debug": "elm-live src/MaximumInterestRateEditor.elm src/TAEG.elm --port=3000 --dir=public/ -- --output=public/app.js --debug",
    "deploy": "npm run build-with-taeg && gh-pages --dist build/",
    "live": "elm-live src/MaximumInterestRateEditor.elm src/TAEG.elm --port=3000 --dir=public/ -- --output=public/app.js",
    "test": "elm-test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alma/maximum-interest-rate-editor.git"
  },
  "bugs": {
    "url": "https://github.com/alma/maximum-interest-rate-editor/issues"
  },
  "homepage": "https://github.com/alma/maximum-interest-rate-editor#readme",
  "author": "Alma",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-live": "^4.0.2",
    "elm-test": "^0.19.1-revision2",
    "gh-pages": "^2.1.1",
    "rimraf": "^3.0.0"
  },
  "dependencies": {
    "elm-analyse": "^0.16.5"
  }
}
